# Swap loaded model to Qwen2.5-Coder-3B-Instruct Q4_K_M

- **Date:** 2026-09-07 20:17
- **Developer:** opencode (big-pickle)
- **Topic:** model-runtime
- **Preceding:** 1240 (context 8192 / qwen3 reasoning diagnosis); 1050 (context 32768 + model switch)

## Request

Swap the loaded model to `qwen2.5-coder-3b-instruct-q4_k_m.gguf`. User approved the
proposed plan and asked to execute it.

## Target artifact (verified via HF tree API)

- Repo: `Qwen/Qwen2.5-Coder-3B-Instruct-GGUF` (official)
- File: `qwen2.5-coder-3b-instruct-q4_k_m.gguf`
- URL: `https://huggingface.co/Qwen/Qwen2.5-Coder-3B-Instruct-GGUF/resolve/main/qwen2.5-coder-3b-instruct-q4_k_m.gguf`
- Size: 2,104,932,800 B (~2.10 GB)
- SHA-256: `724fb256bec1ff062b2f65e4569e871ad2e95ab2a3989723d1769c54294730b7`
- Quant: Q4_K_M

## What was done (executed plan, all subtasks)

Subtask 1 — config:
- `.env` + `.env.example`: MODEL_NAME → `Qwen2.5-Coder-3B-Instruct`, MODEL_URL/MODEL_SHA256
  → official Qwen repo + new hash. (MODEL_QUANT already Q4_K_M — unchanged.)
- `gateway/src/config.ts`: MODEL_NAME default → `Qwen2.5-Coder-3B-Instruct`.
- `scripts/install.sh` + `scripts/update.sh`: MODEL_URL/MODEL_SHA256 defaults + header
  comments → Qwen2.5-Coder-3B-Instruct. `bash -n` clean on all three scripts.

Subtask 2 — UI + docs:
- `gateway/public/index.html`: overview text, /v1/models alias cell, env table
  (LLAMA_CONTEXT_SIZE / MODEL_NAME rows), playground payload model → new name.
- `README.md`: intro + feature bullet, env table, status checklist, getting-started
  (control center, opencode config with 32768/4096 limits + model id), sanity check curl,
  §7 rewritten as a general model-swap runbook (script + API path, rollback, note that the
  model ID changed so laptop opencode config must update).
- `PRD.md`: overview/scope/goals, architecture diagram, requirement #3, §7.5 example
  responses, env table + notes, startup-log sample, chat curl sample, §10.7/§10.7a/§10.7b
  runbook rewritten for the new default, §23 build inputs. Historical/highlighted past
  targets (old Qwen3-1.7B refs) intentionally kept where they record history.

Test fixtures aligned with the new defaults (also reconciled the pre-existing uncommitted
`LLAMA_CONTEXT_SIZE` 32768 default from the 1240/1050 sessions, which had left
`config.test.ts`, `routes.test.ts` and `llama-supervisor.test.ts` expecting 8192):
- `gateway/test/helpers.ts` (fixture name + contextSize 32768), `config.test.ts`,
  `llama-supervisor.test.ts`, `routes.test.ts`, `requests.test.ts`, `v1-proxy.test.ts` —
  model id → `Qwen2.5-Coder-3B-Instruct`, context assertions → 32768.

Verification:
- Ran from a temp copy (`/tmp/opencode/gw`) because `gateway/node_modules` is root-owned
  (no passwordless sudo): `npm ci` ok, `npm run typecheck` clean, `npm test` → **11 files,
  66 tests passed**.
- NOT run: the live model download/swap on the Pi (`sudo ./scripts/install.sh` needs the
  user's password) and a docker rebuild. Remaining user actions: run the swap + tidy and
  update the laptop opencode config model id.

## Notable findings

- The previous "swap to 4-bit" runbook was about quant-only change; this swap changes the
  model ID (`--alias`), which is the one client-visible breaking change.
- Only intentional/historical `Qwen3-1.7B`/`Q8_0` references remain (PRD §23 record of the
  prior build inputs; variants list mentions Q8_0 as a supported config option).
- Repo was already carrying uncommitted changes: `gateway/src/config.ts` (32768 default)
  from the previous session; `git status` also shows the new session file as untracked.

## Open threads

- Execute the live swap (`sudo ./scripts/install.sh [-–-clean-old]` then
  `docker compose up -d`) and re-measure tok/s on the new 3B model.
- Update `~/.config/opencode/opencode.json` on the Arch laptop: model id →
  `Qwen2.5-Coder-3B-Instruct` (context limit 32768).
- Work is uncommitted in the repo (multiple sessions' changes pending commit).